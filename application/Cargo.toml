[package]
name = "application"
edition = "2021"

[dependencies]
application-ports = { path = "../application-ports" }
domain = { path = "../domain" }
domain-shared = { path = "../domain-shared" }

async-trait = "0.1.87"
chrono = "0.4.40"
